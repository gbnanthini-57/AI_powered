def calculate_blast_radius(incident):
    """
    TEMPORARY MOCK.
    Team Member 1 will replace this with real dependency/usage analysis.
    The function name and return structure must stay the same.
    """
    return {
        "affected_files": ["src/routes/checkout.js"],
        "affected_functions": ["processCheckout"],
        "risk_score": "low",
        "similar_patterns_found": 0,
        "notes": "No other endpoints reference this code path.",
    }