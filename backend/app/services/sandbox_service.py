def verify_patch(incident, patch):
    """
    TEMPORARY MOCK.
    Team Member 1 will replace this with real sandbox execution logic
    (applying the patch in an isolated environment, replaying the failing
    request, and running the test suite).
    The function name and return structure must stay the same.
    """
    return {
        "status": "verified",
        "attempts": 1,
        "error_reproduced": True,
        "patch_applied": True,
        "tests_passed": 12,
        "tests_failed": 0,
        "total_tests": 12,
        "verification_message": "Patch resolved the null reference error. All tests passed.",
    }
